export default function Header(){
    return (
         <header className="header">
            <div className="logo">
                <span className="course">Manage users</span>
            </div>
        </header>
    );
};